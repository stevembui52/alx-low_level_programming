#include "main.h"
/**
 * mul - find the poduct of two integers
 *@a:first number to multiply
 *@b:second number to multiply
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
